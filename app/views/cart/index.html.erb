  <% if flash[:notice] %>
    <p><%= flash[:notice] %></p>
  <% end %>

<% cart.each do |game| %>
  <div href="#">
    <%= game.name %><%= link_to "❌", cart_path(game), method: :delete %>
  </div>
<% end %>